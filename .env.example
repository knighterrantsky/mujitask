# Shared local defaults auto-loaded by automation_business_scaffold.
# Runtime DB / MinIO should live in scripts/execution_control/executor.local.env.
# Skill fixed inputs should live in skills/mujitask-tiktok-feishu-sync/skill.local.env.
BROWSER_PROFILES_FILE=config/browser_profiles.json
DEFAULT_PROFILE_REF=local-chrome
AGENT_HOST=127.0.0.1
AGENT_PORT=8110
AGENT_RUN_DIR=runtime/agent_runs
AGENT_RECORDING_DIR=runtime/recordings
