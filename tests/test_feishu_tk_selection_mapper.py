from __future__ import annotations

import pytest

pytest.skip(
    "Archived legacy Feishu TK selection mapper tests were superseded by TikTok domain runtime coverage.",
    allow_module_level=True,
)
